spring.application.name=Enviart
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/Enviart?useSSL=false&allowPublicKeyRetrieval=true}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:root}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=villamiljuan14@gmail.com
spring.mail.password=zwbzuboldxyzzpeb
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Clave de cifrado AES (32 caracteres para AES-256)
# IMPORTANTE: En producción, usar variables de entorno o secretos seguros
app.encryption.key=${ENCRYPTION_KEY:MySecretKey12345MySecretKey12345}

# Configuración HTTPS (descomentar en producción)
# server.ssl.enabled=true
# server.ssl.key-store=classpath:keystore.p12
# server.ssl.key-store-password=changeit
# server.ssl.key-store-type=PKCS12
# server.ssl.key-alias=tomcat

# Redirección HTTP a HTTPS (descomentar en producción)
# server.http.port=8080
server.port=${PORT:8080}
