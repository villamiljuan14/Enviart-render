spring.application.name=Enviart
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/Enviart?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=root
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Clave de cifrado AES (32 caracteres para AES-256)
# IMPORTANTE: En producción, usar variables de entorno o secretos seguros
app.encryption.key=MySecretKey12345MySecretKey12345

# Configuración HTTPS (descomentar en producción)
# server.ssl.enabled=true
# server.ssl.key-store=classpath:keystore.p12
# server.ssl.key-store-password=changeit
# server.ssl.key-store-type=PKCS12
# server.ssl.key-alias=tomcat

# Redirección HTTP a HTTPS (descomentar en producción)
# server.http.port=8080
server.port=8080
